export enum BalanceType {
    balance = 'balance',
    invested = 'invested',
    withdrawn = 'withdrawn',
    referral = 'referral',
    referral_award = 'referral_award',
    holdBalance = 'holdBalance',
}
